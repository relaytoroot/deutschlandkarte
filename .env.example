# ============================================================================
# DEUTSCHLANDKARTE - UMGEBUNGSVARIABLEN
# ============================================================================
# 
# ANLEITUNG:
# 1. Diese Datei KOPIEREN und als ".env" speichern (ohne .example)
# 2. Die Werte für deine Umgebung anpassen
# 3. .env NICHT in Git committen (steht bereits in .gitignore)
#
# ZUHAUSE:  Excel-Daten nutzen
# ARBEIT:   Datenbank angebunden
# ============================================================================

# ============================================================================
# DATENQUELLE: 'excel' ODER 'database'
# ============================================================================
DATA_SOURCE=excel
# Ändern zu "database" wenn du Datenbank-Modus nutzen willst

# ============================================================================
# EXCEL-KONFIGURATION (wenn DATA_SOURCE=excel)
# ============================================================================
EXCEL_PATH=data/Datenmuster_OSNV_Maps.xlsx
# Pfad zur Excel-Datei mit Projekten

# ============================================================================
# DATENBANK-KONFIGURATION (wenn DATA_SOURCE=database)
# ============================================================================
# 
# WICHTIG: Nur eine DATABASE_URL verwenden!
# Auskommentierte Zeilen sind Beispiele für verschiedene DB-Typen
#

# --- PostgreSQL (empfohlen) ---
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name
# Auf der Arbeit z.B.:
# DATABASE_URL=postgresql://admin:firmenpw@db.firma.local:5432/projekte_db

# --- MySQL ---
# DATABASE_URL=mysql+pymysql://username:password@localhost:3306/database_name
# Installation: pip install pymysql

# --- Microsoft SQL Server ---
# DATABASE_URL=mssql+pyodbc://username:password@server_name/database_name?driver=ODBC+Driver+17+for+SQL+Server
# Installation: pip install pyodbc

# --- SQLite (für lokale Tests) ---
# DATABASE_URL=sqlite:///./test.db

# ============================================================================
# UMGEBUNG: 'development' oder 'production'
# ============================================================================
ENVIRONMENT=development
# Ändern zu "production" für Live-System

# ============================================================================
# DEBUG-MODUS
# ============================================================================
DEBUG=True
# Auf False setzen für Production

# ============================================================================
# OPTIONAL: Pfade überschreiben (nur wenn nötig)
# ============================================================================
DEUTSCHLANDKARTE_BASE=
# Optional: Basisordner überschreiben (wenn du NICHT im Repo-Root arbeitest)

# Optional: einzelne Pfade überschreiben (nur wenn nötig)
GERMANY_GEOJSON_PATH=
EUROPE_GEOJSON_PATH=
ICON_DIR=
OUT_HTML=
